# Fanout 综合版验证报告

验证日期：2026-09-23。版本：integrated-1.1.0。

## 已完成验证

- Windows 本地 Go 自动化测试：88 个测试及子测试通过，0 个失败；面板预览测试默认跳过 1 个，随后单独启动并通过浏览器检查。
- `go vet ./...` 静态检查通过。
- Linux amd64、arm64 程序交叉编译通过，使用 Go 1.27.1；安装时校验随包二进制 SHA256。
- 本地 SOCKS5 认证、HTTP CONNECT 转发、UDP 转发及来源限制测试通过。
- 来源保存与删除、冷却重试、导出筛选和凭据转义、只读令牌权限、跨站写入拒绝、密码变更后会话撤销、TLS 证书持久化等测试通过。
- 浏览器检查中文面板，使用文档示例 IP 和测试凭据，确认批量提取生成 VPS 地址、端口及账号密码组成的 S5 链接。示例并非真实在线节点。
- 两个可选公共 SOCKS5 列表的下载地址已检查响应与格式；这不代表列表中每个代理在线或属于住宅 IP。

## 主要修复与整合

- 以 fanout 为主体，增加 VPN Gate、OpenVPN 配置、SOCKS5/HTTP 代理列表、HTTPS 订阅及 VPN Gate CSV 来源管理。
- 出口检测、IP 去重、自动维护、失败重连、批量提取及只读提取 API 纳入单 VPS 中文面板。
- OpenVPN 业务连接及 DNS 绑定 tun0，防止隧道消失后通过 VPS 默认路由直连；高级面板已绑定的失效出口保留为阻断规则。
- 修复 UDP 来源校验及域名响应格式；改善并发状态保存、停止与恢复处理。
- 默认 HTTPS 管理、密码变更撤销旧会话、来源凭据不进入普通列表响应；禁用会覆盖综合版的上游自动更新。
- 住宅属性查询失败返回“未知”，严格模式需要配置兼容检测服务，不把公共代理或 VPN Gate 默认认作住宅 IP。
- 原 Cloudflare Worker 不作为本综合版的部署组件；按照单 VPS 要求重新实现相关管理功能。

## 尚未完成的现场验证

当前环境为 Windows，没有可用 Linux VPS 或 Linux 运行环境。因此未执行 Ubuntu/Debian 安装脚本、真实 OpenVPN 建链、Linux 网络命名空间断线测试、防火墙验证和长期负载测试。也未运行 race 检测或 GitHub Actions；已附 Linux CI 配置及需要 root 的隔离网络测试。

本地测试和交叉编译不能代替这些现场检查。部署后应先用一个出口，检查真实出口 IP、认证、断线阻断、重连和重启恢复，再增加数量。公共来源的速度、可用数量及住宅属性由实际来源决定。

## 包含内容

完整 Go 源码、中文使用说明、安装及管理脚本、Linux amd64/arm64 程序、自动化测试与 Linux CI 配置。二进制校验文件为 BIN_SHA256SUMS，压缩包校验文件单独提供。

## 1.1.0 增量验证

新增国家查询解析、错误响应拒绝、缓存与关闭查询、导出使用检测国家且不绕过住宅筛选的测试。浏览器验证了新版说明和导入类型切换，未见脚本错误。国家 API 字段依据官方文档核对，未在用户 VPS 验证其网络可达性。原版推广链接已移除，MIT 许可证未删改。

## 1.2.0 更新验证（发布前补充）

本地 Go 测试和静态检查通过。新增自定义域名证书匹配、过期拒绝及同一路径证书轮换测试；增加 Linux 上的域名配置失败恢复测试和一键安装下载/校验失败测试。最新 Linux 执行结果以 GitHub Actions 与 Release 页面为准。未在用户的真实域名、DNS 或已有 Nginx/Caddy 上操作；也未直接更新用户 VPS。

1.2.0 Linux CI 已于发布前通过：Go race 测试、go vet、四个 Shell 文件语法检查、Python 证书匹配及配置失败恢复测试、有效包安装/错误校验拒绝/下载失败测试、命名空间隔离测试、双架构编译。记录：https://github.com/y1635331-oss/fanout-integrated/actions/runs/35807913174 。测试使用隔离的模拟安装包，不代表已在用户生产 VPS 完成整机安装。


## v1.3.0 验证记录（2026-09-23）

- 修复 basepath 文件不含前导斜杠导致健康检测 URL 错误。新增读取真实文件格式的 URL 回归测试（无斜杠、带斜杠、空路径）。
- 七个公开来源均 HTTP 200，实际解析候选数量：Au1rxx 766、awesome 47、freesub 1155、proxy-pool 636、freeSub 197、snakem982 32、NoMoreWalls 218。该数字是当时样本，不保证在线，不固定展示为在线数量。
- 新增混合坏行容错、跨标签去重、订阅配置剥离、拒绝 insecure TLS、独立内核代理传输/终止不直连测试。
- 本地 Go 单元测试与静态检查通过；Linux CI 结果以仓库 Actions 为准。未直接操作用户 VPS。
