# Fanout 综合版 1.0

以 fanout 为基础的单台 VPS 多出口 SOCKS5 管理工具，适用于 Ubuntu/Debian。将获取到的 VPN/代理出口映射为 VPS 上的独立 S5 端口。

## 你得到什么

- 一个中文综合面板：获取来源、批量建出口、手动选节点、自动补充、状态和日志、批量提取。
- VPN Gate 自动获取；面板可选添加 Proxifly、TheSpeedX 公共 SOCKS5 列表。
- 导入自己的 OpenVPN 配置、SOCKS5/HTTP/HTTPS 上游代理，或 HTTPS 订阅。
- 每个出口独立账号密码；重连和重启保留 S5 端口及凭据。
- 出口 IP 实际探测、重复出口去重、失败冷却、自动重连。
- S5 链接、CSV、JSON 导出；独立只读提取令牌。
- 默认 HTTPS 管理界面。保留原 fanout 的 Xray/3x-ui/xray-cf-lite 高级管理入口。

连接关系：

```text
客户端 → VPS公网IP:独立端口 → OpenVPN 或上游代理 → 对方看到的出口IP
```

例如 `socks5://账号:密码@VPS公网IP:23001`。链接里的 IP 是你的 VPS 接入地址，目标网站看到的是后面的出口 IP，两者不是同一个地址。

## 安装

建议 Ubuntu 22.04/24.04 或 Debian 12/13，root 权限，systemd，开放 `/dev/net/tun`，OpenVPN 2.5+。完整包包含 Linux amd64 和 arm64 程序，无须在 VPS 安装 Go。

1. 将完整安装包上传 VPS 并解压。
2. 进入 `fanout-integrated` 目录。
3. 执行：

```bash
sudo bash install.sh
```

需要换管理端口或调整最大出口数：

```bash
sudo WEB_PORT=8899 MAX_EXITS=20 bash install.sh
```

默认最大 20 个槽位，可设置 1–254。建议先开 3–5 个验证 VPS 性能、带宽和节点可用性；上限不是性能承诺。安装仅修改综合版的服务和数据目录，不运行原项目的云端安装脚本，不修改系统日志保留策略。

默认管理端口为 **8899/TCP**。S5 TCP 端口从 **20000–59999** 随机分配，实际端口在面板中查看；云安全组和 VPS 防火墙需要放行你要使用的端口。直接使用 SOCKS5 UDP 时还需要放行动态 UDP 中继端口；防火墙未放行会导致 TCP 正常而 UDP 失败。

首次安装打印地址、随机口令和证书 SHA-256 指纹。默认自签 HTTPS 证书需要通过 SSH 输出核对后手动信任。配置自己的域名证书可以避免浏览器提示，见下文。

如果母机安装了 WARP、位于 NAT 后面或探测地址不是实际接入 IP，在 `/etc/default/fanout-integrated` 写入：

```text
FANOUT_PUBLIC_IP=你的VPS公网IPv4
```

然后执行 `sudo fanoutctl restart`。自动 IP 探测无法保证识别 NAT 映射或 WARP 前面的真实公网地址。

## 使用流程

1. 打开综合面板，确认“VPS 连接地址”正确。
2. 等待内置 VPN Gate 加载；也可以点击添加公共来源，或展开“添加来源 / 导入配置”。
3. 选择地区和新建数量，点击“批量建立出口”。同一节点不会重复占用多个槽位。
4. 等待状态为“可用”，查看真实出口 IP。连接中的出口和失败出口不会导出。
5. 点击“批量提取”，选择 S5 链接、CSV 或 JSON；也可在某一行单独提取。
6. 需要长期维护时开启“自动补充”，保存目标数量、地区和刷新间隔。

自动补充按槽位数维护；失败槽位持续重连，不会无限新建槽位。降低目标数量不会删除已有出口。自动补充开启时，手动停止的出口可能由新槽位补回；想暂停补充请先关闭该选项。

“换 IP”会断开旧出口并重连，不是保活迁移，已有连接可能中断。免费节点是否可用、延迟和带宽取决于第三方，不能保证目标数量始终全部在线。

## 来源格式

### SOCKS5 / HTTP / HTTPS

一行一个完整地址，可混合：

```text
socks5://username:password@proxy.example.com:1080
http://username:password@proxy.example.com:8080
https://username:password@proxy.example.com:8443
socks5://proxy.example.com:1080
```

账号密码中的特殊字符要使用 URL 编码。HTTP/HTTPS 上游需支持 CONNECT，HTTPS 表示连接代理服务器本身也使用 TLS。

选择专用“SOCKS5 列表”或“HTTP 列表”类型时，也接受 `IP:端口`。每个来源最多载入前 2000 条有效代理，最多保存 50 个来源。公共来源添加时只读取列表，不会立即启动所有代理。国家不确定时留空显示 UN；来源填写的国家是标签，不代表已经核实地理位置。

公共来源入口已按以下仓库的公开文本格式接入：

- [Proxifly](https://github.com/proxifly/free-proxy-list)
- [TheSpeedX](https://github.com/TheSpeedX/PROXY-List)

这些都是第三方公共资源，不是住宅 IP 保证，也不是可用率保证。使用你有权使用的来源。

### OpenVPN

选择 OpenVPN 类型，上传或粘贴一个 `.ovpn` 文件；一个来源对应一套配置。支持常见的内嵌 CA、证书、私钥。外部文件引用、脚本、插件和配置嵌套会被拒绝，需要先转换成自包含的配置。

可填写该配置对应的 VPN 用户名和密码；留空使用 VPN Gate 的 `vpn/vpn`。自有 VPN 应按服务商提供的凭据填写。导入的 `.ovpn` 必须是客户端配置。

### HTTPS 订阅

填写 HTTPS URL，并选择返回内容对应的类型。订阅自动按维护策略定时刷新；拉取失败保留上次成功内容，并在来源列表显示错误。

支持纯文本代理列表、单个 OpenVPN 文件，以及 VPN Gate 格式 CSV。暂不直接解析 Clash、V2Ray Base64、WireGuard、Hysteria2 订阅；原有 Xray 节点管理和这里的来源导入是不同功能。

## 住宅属性检测

没有配置检测接口时，出口属性为“住宅未验证”。不会因为连通、ISP 名称、国家标签或历史成功而自动判为住宅。

自定义 HTTPS 检测 URL 支持 `{ip}` 占位，必须返回：

```json
{"ip":"待检测的真实出口IP","type":"residential","isp":"运营商名称","country":"JP"}
```

`type` 仅接受 `residential`、`datacenter`、`unknown`；IP 必须与请求检测的出口一致。其他供应商的返回格式需要先适配，不能直接填入任意网站页面地址。

开启严格住宅模式需要先配置接口。未知、检测异常和机房分类都不会作为新出口上架；导出也过滤已有的非住宅分类。既有已连接客户端不会因修改筛选开关立即断开。该功能信任你提供的检测服务，没有内置付费 API 密钥。

## 程序化提取

点击“生成提取令牌”后会生成新的只读令牌，旧令牌立即失效。只有登录管理员能生成令牌；它只能读取 `/api/proxies`，不能修改配置。

```bash
curl --cacert ./web.crt \
  -H 'Authorization: Bearer 你的提取令牌' \
  'https://VPS地址:8899/随机路径/api/proxies?format=json&country=JP&count=5'
```

使用自己的受信任证书时可省略 `--cacert`。`web.crt` 可通过 SSH 从 `/var/lib/fanout-integrated/web.crt` 复制。支持 `format=text|csv|json`；不填写地区或数量时返回全部符合条件的已检测出口。令牌放在请求头，不放在 URL。

## 运维

```bash
sudo fanoutctl info       # 地址、口令、默认证书指纹
sudo fanoutctl status
sudo fanoutctl log
sudo fanoutctl restart
sudo fanoutctl password   # 修改管理口令并重启
sudo fanoutctl stop
```

数据目录：`/var/lib/fanout-integrated`。配置、状态和来源文件含代理凭据，目录权限 0700、文件权限 0600；备份时按敏感配置保管。

自定义管理证书在 `/etc/default/fanout-integrated` 设置：

```text
FANOUT_TLS_CERT=/你的目录/fullchain.pem
FANOUT_TLS_KEY=/你的目录/privkey.pem
```

然后重启服务。默认自签证书有效期一年，更新证书需要重新启动服务。

直接生成 S5 不需要 Xray。高级节点管理可复用现有 3x-ui、xray-cf-lite；自建 Xray 模式需要将可执行 Xray 安装至 `/var/lib/fanout-integrated/bin/xray` 或 `/usr/local/bin/xray`。本安装包不捆绑第三方 Xray 二进制。

升级：下载新的综合版包，校验后再次执行 `install.sh`；保留设置和状态。安装器备份上一版程序，可用 `fanoutctl rollback` 回退程序，数据格式是否兼容应按版本说明判断。原版在线更新被禁用，避免覆盖综合版。

## 验证范围

具体测试结果见 `验证报告.md`。本次交付不包含连接你的真实 VPS、修改云安全组、长期公网压力测试或第三方节点可用率保证。

提供了 Linux 隔离测试，可在有 root、iproute2、curl、Go 的测试 VPS 上运行：

```bash
sudo FANOUT_NETNS_TEST=1 /你的Go路径/go test -run TestNamespaceFailsClosedWithWorkingDefaultRoute -v
```

它只创建临时测试命名空间和 veth，不连接第三方 VPN，用可工作的基础默认路由检查业务连接是否拒绝回退。建议在专用测试环境运行。

## 来源与许可

基于用户提供的 fanout 源码修改，保留其 MIT LICENSE；原项目说明保存在 `README-upstream.md`。综合版采用另一个工具的集中展示、节点池、筛选和提取思路；没有运行或复用其公开返回管理密码的 Worker 脚本接口。此版本为用户选定的单台 VPS 方案，未加入 Cloudflare 多机控制端。

## 1.1.0 国家识别与面板帮助

新增国家与运营商识别，默认开启，可在“国家识别与住宅筛选”中关闭。升级后保存设置，点击“重新识别”更新已有在线出口。系统只向 https://ipwho.is/ 发送出口 IP；成功缓存 24 小时，失败缓存 10 分钟。不会发送账号密码。服务限额和可用性以其官方说明为准。

公共列表在建立出口前可能没有国家，显示“待连接识别”；先选“不限地区”建立少量出口。识别后的地区用于出口显示和导出国家筛选，不会把整个来源里的其他候选伪装成同一个国家。住宅属性使用单独检测接口，没有结果时保持“住宅未验证”。

导入表单按类型显示说明和示例，另有首次操作指南、来源获取表格、支持格式、排错、令牌和升级说明。登录页与高级面板已去掉原项目推广链接；MIT 版权声明保留。
